name: Deploy Project

on:
  push:
    branches: [main, master, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

permissions:
  contents: read

jobs:
  macos-deploy:
    name: macOS Deploy

    runs-on: macos-15

    timeout-minutes: 30

    environment: production-macos

    env:
      CODESIGN_OK: ${{ secrets.MACOS_SIGNING_APPLICATION_IDENTITY != '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up macOS Signing Certificates
        uses: ./.github/actions/setup-macos-codesigning
        id: codesign
        if: ${{ env.CODESIGN_OK == 'true' }}
        with:
          MACOS_SIGNING_APPLICATION_IDENTITY: ${{ secrets.MACOS_SIGNING_APPLICATION_IDENTITY }}
          MACOS_SIGNING_CERT: ${{ secrets.MACOS_SIGNING_CERT }}
          MACOS_SIGNING_CERT_PASSWORD: ${{ secrets.MACOS_SIGNING_CERT_PASSWORD }}

      - name: Install dependencies via Homebrew
        run: brew install cmake

      - name: Download dependencies
        run: cmake -P scripts/download-deps.cmake

      - name: Build libobs
        run: scripts/build-obs-studio-macos.sh

      - name: Configure
        env:
          CODESIGN_IDENT: ${{ steps.codesign.outputs.CODESIGN_IDENT }}
          CODESIGN_TEAM: ${{ steps.codesign.outputs.CODESIGN_TEAM }}
        run: cmake --preset macos

      - name: Build
        run: cmake --build --preset macos

      - name: Package
        run: |
          mkdir -p release
          cmake --install build_macos --prefix release

      - name: Upload Installer
        uses: actions/upload-artifact@v6
        with:
          name: ${{ github.event.repository.name }}-installer-macos-universal-${{ github.run_number }}
          path: release/*.pkg
          if-no-files-found: error
